enum LoginMethod { mobile, email }

enum RequestMethod { get, post, put, delete, patch }

enum CacheKeys {
  firstVisit,
  profileImage,
  loggedIn,
  remember,
  language,
  theme,
  userToken,
  currency,
  fcmToken,
  appSignature,
  name,
  mobile,
  email,
  splashBg,
  isActive,
  activeBiometric,
  countryCode,
  dialCode,
  userType,
  notifiable,
}

enum AppLanguages { ar, en }

enum UserType { merchant, company }

enum TosterTypes { warning, sucsses, failed }
